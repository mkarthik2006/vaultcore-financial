export default function Footer() {
  return (
    <footer className="border-top py-3 px-4 small vc-text-muted text-center">
      VaultCore &copy; {new Date().getFullYear()}. For authorized use only. All activity is monitored for compliance.
    </footer>
  );
}
